# pkill -HUP is for older version of zinit that doesn't start process
# in its own process group
exec: |
  sh -c '
  pkill -HUP vector

  export NODE=$(noded -id)
  export NETWORK=$(noded -net)
  exec vector -c /etc/vector/vector.yaml
  '
log: stdout
after:
  - noded
signal:
  stop: SIGHUP
