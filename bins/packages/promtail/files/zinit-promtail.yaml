exec: |
  sh -c 'NODEID=$(noded -id) NETWORK=$(noded -net) exec promtail \
            -config.file /etc/promtail/promtail.yaml \
            -server.disable \
            -config.expand-env=true \
            -client.url http://explorer.devnet.grid.tf:3100/loki/api/v1/push \
  '
log: stdout
