name: Release (development)
on:
    # push:
    # Do not trigger this event if it's a new tag
    # We use tag for release (testing, production), not development
    # tags-ignore:
    # - v*
  [push]

jobs:
  build:
    name: Build and upload
    runs-on: ubuntu-latest
    steps:

    - name: Set up Go 1.13
      uses: actions/setup-go@v1
      with:
        go-version: 1.13
      id: go

    - name: Checkout code into the Go module directory
      uses: actions/checkout@v1

    - name: Build binaries
      run: |
        cd cmds
        make
      env:
        GO111MODULE: on

    - name: Collecting files
      run: |
        mkdir -p ${{ github.workspace }}/archive/{bin,etc/zinit}
        cp bin/* ${{ github.workspace }}/archive/bin/
        cp -r zinit/* ${{ github.workspace }}/archive/etc/zinit

    - name: Publish flist (${{ github.sha }})
      if: success()
      uses: threefoldtech/publish-flist@master
      with:
        action: publish
        user: tf-autobuilder
        root: archive
        token: ${{ secrets.HUB_JWT }}
        name: zos-${{ github.sha }}.flist

    - name: Symlink flist (development)
      if: success()
      uses: threefoldtech/publish-flist@master
      with:
        action: symlink
        user: tf-autobuilder
        token: ${{ secrets.HUB_JWT }}
        name: zos-${{ github.sha }}.flist
        target: zos:development:latest.flist

