entryPoints:
  web:
    address: ":80"

  web-secure:
    address: ":443"
providers:
  file:
    directory: "%s/proxy"
    watch: true
certificatesResolvers:
  le:
    acme:
      # is there a farmer mail we can use?
      email: email@notexamplebecausetraefikrefusesexample.com
      storage: %[1]s/traefik/acme.json
      httpChallenge:
        # used during the challenge
        entryPoint: web
